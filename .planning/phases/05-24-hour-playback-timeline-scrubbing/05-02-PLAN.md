---
phase: 05-24-hour-playback-timeline-scrubbing
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - client/src/components/TimelineScrubber.tsx
  - client/src/components/PlaybackControls.tsx
  - client/src/components/PlaybackPlayer.tsx
  - client/src/pages/PlaybackPage.tsx
  - client/src/index.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-04
user_setup: []
must_haves:
  truths:
    - "Timeline scrubber renders 24-hour visual activity bar showing green recorded intervals"
    - "Clicking or dragging on the timeline scrubs and seeks playback to that exact timestamp"
    - "User can play, pause, step forward/backward (+5s/-5s), and adjust playback speed (0.5x to 8x)"
    - "Date picker enables switching days to view past historical recordings"
  artifacts:
    - path: "client/src/components/TimelineScrubber.tsx"
      provides: "24-hour visual timeline scrubber with recorded spans and draggable playhead"
      exports: ["TimelineScrubber"]
    - path: "client/src/components/PlaybackControls.tsx"
      provides: "VCR/DVR controls for play, pause, step, and speed"
      exports: ["PlaybackControls"]
    - path: "client/src/components/PlaybackPlayer.tsx"
      provides: "HTML5 video player streaming fMP4 chunks from MediaMTX"
      exports: ["PlaybackPlayer"]
    - path: "client/src/pages/PlaybackPage.tsx"
      provides: "Full playback dashboard page with camera selector and timeline"
      exports: ["PlaybackPage"]
  key_links:
    - from: "client/src/components/TimelineScrubber.tsx"
      to: "client/src/components/PlaybackPlayer.tsx"
      via: "onSeek callback updates player currentTime"
      pattern: "onSeek"
    - from: "client/src/pages/PlaybackPage.tsx"
      to: "/api/playback/timeline"
      via: "fetches 24-hour recorded spans"
      pattern: "fetch.*api/playback/timeline"
---

<objective>
Implement the React 24-hour visual timeline scrubber and DVR playback controls, enabling historical surveillance footage review (PLAY-01, PLAY-02, PLAY-04).

Output: `TimelineScrubber`, `PlaybackControls`, `PlaybackPlayer`, and `PlaybackPage` components.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-24-hour-playback-timeline-scrubbing/05-RESEARCH.md
@.planning/phases/05-24-hour-playback-timeline-scrubbing/05-01-PLAN.md
</context>

<threat_model>
<threat>
T-05-04: Client memory leaks during prolonged timeline scrubbing
Severity: low
Mitigation: Properly clean up video event listeners and throttle scrubbing callbacks with requestAnimationFrame.
</threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build 24-Hour Visual Timeline Scrubber component</name>
  <files>client/src/components/TimelineScrubber.tsx</files>
  <action>
    - Create `client/src/components/TimelineScrubber.tsx`:
      - 24-hour horizontal track (00:00 to 24:00, 86,400 seconds / 1440 minutes).
      - Renders recorded spans (`spans: TimelineSpanDto[]`) as colored blocks positioned proportionately along the bar.
      - Displays hourly time ruler markings (00:00, 02:00, ..., 22:00, 24:00) with minor tick lines.
      - Interactive playhead cursor indicating current playback time.
      - Click-to-seek and drag-to-scrub support (`PLAY-02`): computes clicked/dragged position percentage and converts to timestamp, calling `onSeek(date)`.
      - Hover time tooltip displaying time under cursor.
  </action>
  <verify>TypeScript compiles cleanly with no errors</verify>
  <acceptance_criteria>
    - Timeline scrubber renders 24-hour ruler and spans accurately
    - Supports click and drag scrubbing
  </acceptance_criteria>
  <done>TimelineScrubber implemented</done>
</task>

<task type="auto">
  <name>Task 2: Build DVR Playback Controls and HTML5 Video Player</name>
  <files>client/src/components/PlaybackControls.tsx, client/src/components/PlaybackPlayer.tsx</files>
  <action>
    - Create `client/src/components/PlaybackControls.tsx`:
      - Play / Pause toggle button (`PLAY-04`).
      - Step backward (-5s) and step forward (+5s) buttons.
      - Playback speed dropdown / cycle button: 0.5x, 1x, 2x, 4x, 8x.
      - Date selector input allowing day switching.
      - Current timestamp and duration indicator.
    - Create `client/src/components/PlaybackPlayer.tsx`:
      - HTML5 `<video>` element consuming MediaMTX fMP4 stream (`PLAY-03`).
      - Synchronizes video playback progress with current playhead.
      - Handles video load, error, and buffer states.
  </action>
  <verify>Components pass type checking</verify>
  <acceptance_criteria>
    - Play, pause, step, and variable speed controls work
    - Video player streams fMP4 and tracks current playback time
  </acceptance_criteria>
  <done>PlaybackControls and PlaybackPlayer implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build PlaybackPage dashboard and export components</name>
  <files>client/src/pages/PlaybackPage.tsx, client/src/index.ts</files>
  <action>
    - Create `client/src/pages/PlaybackPage.tsx`:
      - Top navigation bar with camera selector dropdown, date picker, and navigation link back to Live View.
      - Integrates `PlaybackPlayer`, `TimelineScrubber`, and `PlaybackControls`.
      - Fetches `/api/playback/timeline` when camera or date changes.
      - Resolves stream URL via `/api/playback/stream` when seeking.
    - Update `client/src/index.ts` to export all playback components.
  </action>
  <verify>npx tsc -p client/tsconfig.json --noEmit passes</verify>
  <acceptance_criteria>
    - PlaybackPage integrates player, timeline, and controls
    - Client build and typecheck pass
  </acceptance_criteria>
  <done>PlaybackPage implemented and exported</done>
</task>

</tasks>
