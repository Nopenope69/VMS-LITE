---
phase: 01-foundation-licensing-event-bus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - prisma/schema.prisma
  - src/server.ts
  - src/db/prisma.ts
autonomous: true
requirements:
  - AUTH-01
  - EVT-01
user_setup: []
must_haves:
  truths:
    - "Clean-room TypeScript Fastify server starts and listens on configured port"
    - "Prisma schema defines User and Event models with zero VigilOne enterprise models"
    - "Prisma client compiles and generates type-safe database accessors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "fastify"
    - path: "prisma/schema.prisma"
      provides: "PostgreSQL database schema for users and events"
      contains: "model Event"
    - path: "src/server.ts"
      provides: "Fastify server bootstrap"
      min_lines: 30
    - path: "src/db/prisma.ts"
      provides: "Singleton Prisma client instance"
      exports: ["prisma"]
  key_links:
    - from: "src/server.ts"
      to: "src/db/prisma.ts"
      via: "import prisma client"
      pattern: "import.*prisma"
---

<objective>
Scaffold the clean-room Basic VMS backend application, TypeScript build configuration, PostgreSQL database schema with Prisma ORM, and Fastify server skeleton.

Purpose: Provide a stable, lightweight runtime and relational database layer that isolates Basic VMS completely from VigilOne code and multi-tenant overhead.
Output: Initialized `package.json`, `tsconfig.json`, `vitest.config.ts`, `prisma/schema.prisma`, and `src/server.ts`.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-licensing-event-bus/01-RESEARCH.md
</context>

<threat_model>
<threat>
T-01-01: VigilOne IP/schema contamination
Severity: high
Mitigation: Create minimal, clean-room schema containing only User (id, username, passwordHash, role, createdAt) and Event (id, cameraId, timestamp, type, source, severity, metadata, createdAt). Absolutely no multi-tenant, organization, or evidence chain tables.
</threat>
<threat>
T-01-02: Secret exposure in repository
Severity: high
Mitigation: Add comprehensive .gitignore ensuring .env, local SQLite/Postgres secrets, private keys, and build artifacts are excluded.
</threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project manifest and TypeScript toolchain</name>
  <files>package.json, tsconfig.json, vitest.config.ts, .gitignore</files>
  <action>
    Configure `package.json` with permissive (MIT) dependencies:
    - `fastify`, `@fastify/cors`, `@fastify/jwt`, `@noble/ed25519`, `bcrypt`, `zod`, `@prisma/client`, `dotenv`
    - Dev dependencies: `typescript`, `@types/node`, `@types/bcrypt`, `prisma`, `tsx`, `vitest`
    - Configure `tsconfig.json` targeting Node 20 (ES2022 / NodeNext module resolution).
    - Configure `vitest.config.ts` for automated test execution.
    - Update `.gitignore` to ignore `.env`, `dist`, `node_modules`, and test coverage.
  </action>
  <verify>npm run build passes or tsc --noEmit verifies configuration</verify>
  <acceptance_criteria>
    - package.json has dependencies and type: "module" or CommonJS configured
    - tsconfig.json exists and specifies target: "ES2022"
    - vitest.config.ts exists and specifies test matchers
  </acceptance_criteria>
  <done>Clean package manifest and TypeScript build toolchain ready</done>
</task>

<task type="auto">
  <name>Task 2: Define clean-room PostgreSQL schema with Prisma</name>
  <files>prisma/schema.prisma, src/db/prisma.ts</files>
  <read_first>.planning/phases/01-foundation-licensing-event-bus/01-RESEARCH.md</read_first>
  <action>
    Create `prisma/schema.prisma`:
    - Database provider: "postgresql" (with DATABASE_URL env fallback)
    - `User` model: `id` (String UUID), `username` (String unique), `passwordHash` (String), `role` (Enum: ADMIN, VIEWER), `createdAt`, `updatedAt`
    - `Event` model: `id` (String UUID), `cameraId` (String optional), `timestamp` (DateTime), `type` (String), `source` (String), `severity` (String default "info"), `metadata` (Json), `createdAt` (DateTime default now)
    - Create `src/db/prisma.ts` exporting singleton `PrismaClient`.
  </action>
  <verify>npx prisma generate produces client types without error</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains "model User" with role enum ADMIN, VIEWER
    - prisma/schema.prisma contains "model Event" with metadata Json field
    - prisma/schema.prisma contains NO references to Tenant, Organization, or Evidence
  </acceptance_criteria>
  <done>PostgreSQL schema defined and Prisma client generated</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Fastify server bootstrap</name>
  <files>src/server.ts, src/index.ts</files>
  <read_first>src/db/prisma.ts</read_first>
  <action>
    Create `src/server.ts`:
    - Export `createServer()` function returning configured Fastify instance.
    - Register `@fastify/cors` and JSON body parsing.
    - Register healthcheck endpoint: `GET /health` returning `{ status: "ok", service: "basic-vms", timestamp }`.
    - Create `src/index.ts` to bind server to `PORT` (default 3000) and `HOST` (0.0.0.0).
  </action>
  <verify>npm test or tsx src/server.ts syntax check</verify>
  <acceptance_criteria>
    - src/server.ts exports createServer
    - GET /health endpoint implemented
    - Server starts cleanly without throwing
  </acceptance_criteria>
  <done>Fastify server boots and serves healthcheck</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles cleanly with `npx tsc --noEmit`
- [ ] `npx prisma generate` creates client types
- [ ] Healthcheck route returns 200 OK
</verification>

<success_criteria>
- Project toolchain initialized with 100% permissive dependencies
- Clean-room Prisma schema created and compiled
- Fastify server operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-licensing-event-bus/01-01-SUMMARY.md`
</output>
