---
phase: 08-operator-role-granular-rbac
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - client/src/context/AuthContext.tsx
  - client/src/components/UserManagementModal.tsx
  - client/src/components/OperatorBanner.tsx
  - client/src/pages/LiveViewPage.tsx
  - client/src/pages/PlaybackPage.tsx
  - client/src/index.ts
autonomous: true
requirements:
  - EXT-01
user_setup: []
must_haves:
  truths:
    - "Client UI exposes AuthContext with user role (ADMIN, OPERATOR, VIEWER) and token persistence"
    - "Operators see a distinct 'Operator Station' status badge in Kinetic Amber (#fb923c)"
    - "Admin users can open User & Access Management modal to create operators and assign per-camera permissions"
    - "Operator views are filtered so only permitted cameras appear in Live Grid and Playback timeline selector"
    - "Administrative mutation controls are hidden when logged in as an Operator"
  artifacts:
    - path: "client/src/context/AuthContext.tsx"
      provides: "Authentication context with role and permission awareness"
      exports: ["AuthProvider", "useAuth"]
    - path: "client/src/components/UserManagementModal.tsx"
      provides: "Admin modal to manage users and per-camera permission checkboxes"
      min_lines: 80
    - path: "client/src/components/OperatorBanner.tsx"
      provides: "Visual operator shift mode banner"
      min_lines: 20
  key_links:
    - from: "client/src/pages/LiveViewPage.tsx"
      to: "client/src/context/AuthContext.tsx"
      via: "useAuth hook to check role and filter camera list"
      pattern: "useAuth"
---

<objective>
Deliver the React client-side experience for 3-tier RBAC (`ADMIN`, `OPERATOR`, `VIEWER`), including the Operator Station workstation mode, permission-filtered camera navigation, and the Admin User & Camera Permission Management UI.

Purpose: Provide an intuitive, low-complexity interface tailored for Indian security guard stations (Operator mode) while giving facility admins full control over camera access permissions.
Output: `AuthContext.tsx`, `UserManagementModal.tsx`, `OperatorBanner.tsx`, and updated `LiveViewPage.tsx` and `PlaybackPage.tsx`.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-operator-role-granular-rbac/08-RESEARCH.md
@.planning/phases/08-operator-role-granular-rbac/08-01-PLAN.md
@client/src/pages/LiveViewPage.tsx
@client/src/pages/PlaybackPage.tsx
</context>

<tasks>

<task type="auto" name="auth-context-and-operator-banner">
  <action>
    1. Create `client/src/context/AuthContext.tsx`:
       - Define `User` interface with `id`, `username`, `role: 'ADMIN' | 'OPERATOR' | 'VIEWER'`.
       - Provide `AuthProvider` with token storage in `localStorage`, login, logout, and current user fetch (`GET /api/auth/me`).
       - Expose `useAuth()` hook returning `{ user, token, role, isAdmin, isOperator, isViewer, login, logout }`.
    2. Create `client/src/components/OperatorBanner.tsx`:
       - Styled in Palette 1 (Kinetic High Contrast: Solar Amber `#fb923c` on Slate/Obsidian background).
       - Displays: `[OPERATOR STATION - GUARD SHIFT MODE]` with logged-in username and restricted access notice.
  </action>
  <verify>
    Run `npx tsc --project client/tsconfig.json` to verify compilation.
  </verify>
</task>

<task type="auto" name="user-and-permission-management-modal">
  <action>
    1. Create `client/src/components/UserManagementModal.tsx`:
       - Only accessible by `ADMIN`.
       - Displays user list fetched from `GET /api/auth/users`.
       - Allows creating new users (`username`, `password`, `role`).
       - When selecting an `OPERATOR`, renders a camera permission matrix:
         - Fetches all cameras and user's permissions (`GET /api/auth/users/:id/permissions`).
         - Displays each camera with checkboxes:
           - `Live View`
           - `Playback`
           - `PTZ Control`
           - `Clip Export`
         - Saves changes to `PUT /api/auth/users/:id/permissions`.
       - Uses Palette 1 styling: Ion Blue `#4fc3f7` accents, Dark Slate surface, clean high-contrast checkboxes.
  </action>
  <verify>
    Run `npx tsc --project client/tsconfig.json` to verify compilation.
  </verify>
</task>

<task type="auto" name="page-integration-and-role-gating">
  <action>
    1. Update `client/src/pages/LiveViewPage.tsx`:
       - Wrap or consume `useAuth()`.
       - Render `OperatorBanner` when `user?.role === 'OPERATOR'`.
       - Render "Users & Access" button in header only when `isAdmin === true`.
       - Filter camera list to only permitted cameras.
    2. Update `client/src/pages/PlaybackPage.tsx`:
       - Ensure camera selector dropdown only displays cameras permitted for playback.
       - If user is Operator without playback permission for a camera, disallow selection.
    3. Update `client/src/index.ts` to export new components.
  </action>
  <verify>
    Run `npm run build` or `npx tsc --noEmit && npx tsc --project client/tsconfig.json` to confirm 0 compilation errors across both server and client.
  </verify>
</task>

</tasks>

<must_haves>
1. `AuthContext` provides authenticated user role and permission helper flags.
2. `UserManagementModal` allows admins to create operators and assign per-camera permissions.
3. `OperatorBanner` renders on guard workstations when `user.role === 'OPERATOR'`.
4. Camera lists in Live View and Playback are scoped strictly to permitted cameras for Operators.
5. All client and server TypeScript builds pass with 0 errors.
</must_haves>
